[package]
name = "simpledb"
version = "0.1.0"
edition = "2024"



[dependencies]
rand = "0.10.0-rc.7"
zerocopy = { version = "0.8", features = ["derive"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }
